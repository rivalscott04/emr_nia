/** Default page size for many list endpoints (matches backend defaults where applicable). */
export const LIST_LIMIT_DEFAULT = 50

/** Master lists, audit logs, bulk exports. */
export const LIST_LIMIT_MASTER = 100

/** Master ICD grid. */
export const LIST_LIMIT_ICD = 200

/** Small recent lists (e.g. dashboard). */
export const LIST_LIMIT_RECENT = 5
